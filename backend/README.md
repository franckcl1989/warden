# Warden backend

FastAPI + SQLAlchemy + PostgreSQL 18 backend for the Warden 0.1.0 hardware
monitoring and management platform. See `docs/` for the approved design
baseline; `contracts/` is the machine-readable source of truth.
