# Warden 项目协作规则

本文件对所有参与本项目的人和 AI agent 生效。

## 开始工作前

按顺序阅读：

1. `docs/README.md`
2. `docs/SOURCE_BASELINE.md`
3. `docs/PROJECT_SPEC.md`
4. `docs/TRACEABILITY.md`
5. `contracts/capabilities.json`
6. `contracts/metrics.json`
7. `contracts/events.json`
8. `contracts/operations.json`
9. `contracts/alert-rules.json`
10. `contracts/http-api.json`
11. `contracts/error-codes.json`
12. `contracts/hardware-targets.json`
13. `contracts/hardware-certification.schema.json`
14. `docs/DEVELOPMENT_PLAYBOOK.md`
15. 与当前任务直接相关的专题设计文档
16. `docs/DECISION_LOG.md`

开始修改前运行 `pwsh -File scripts/check-design.ps1`。基线校验失败时不得继续扩展实现。

## 不可违反的范围规则

- 项目核心只有一句话：对一个小型私有化机房的硬件设备进行监控和管理。
- 人工触发的硬件业务操作，必须能逐项追溯到飞书《边端硬件监控及功能实现项》；没有来源编号的硬件操作不得实现。
- 文档外只能补充实现原始需求所必需的平台支撑，例如认证、设备接入、任务执行、凭据保护、文件存储和审计。
- 平台支撑必须指出它承载的来源需求；能通过部署配置或内部实现解决的事项，不得再增加用户页面、业务 API 或独立业务模块。
- 不做机房/机柜、组织/租户、通用 CMDB、工单、通知中心、自动化编排、容量规划或其他未要求的业务模块。
- 0.1.0 只使用持久化卷，不建设数据库备份服务、PITR、灾备编排、备份页面或 RPO/RTO 产品能力；不要把设备“配置备份/恢复”误解成平台数据库备份。
- 不建设用户可编辑的通用告警规则，不为 CPU、内存、容量、错误计数或 PoE 比例发明固定阈值；当前问题只来自设备明确的当前状态/告警、离线和数据过期，设备日志事件不得自动变成当前告警。
- 小型单机基线使用 PostgreSQL 持久化任务队列，不引入 Redis、Celery 或消息 outbox；未经新 ADR 不得增加中间件。
- 现场设备数量和活跃指标序列未确认前，不得写死 100 台、30,000 序列、500 GiB 等容量承诺。
- 不得把设备不支持、未配置、超时或返回不确定的结果伪装成成功。

## 实施规则

- 需求编号、能力键、适配器方法、API、页面和测试必须保持可追溯。
- 指标类型/单位、事件字段、动作参数/风险/超时/验证和告警策略只取 `contracts/`；没有定义时停止并记录待决策，不得自行猜测。
- 只通过统一设备适配器访问硬件；页面、API 和任务代码不得直接调用厂商协议。
- 监控读取与变更操作严格分离；具有副作用的操作必须进入持久化任务队列。
- 任务由 PostgreSQL 条件领取和租约恢复；数据库记录本身就是执行授权，不维护第二套消息队列状态。
- KVM/DSM/Web/SSH/Telnet 连接按 launch 契约签发一次性票据并审计，不进入副作用任务队列。
- 高风险操作必须保留权限校验、操作预览、二次确认、幂等保护、结果验证和审计。
- Worker 在设备调用前必须持久化 dispatch fence；有副作用动作 fence 后崩溃只允许回读核验，禁止重复调用设备；纯读取只按契约创建新 attempt。
- 设备凭据、配置备份和可能含敏感信息的支持日志不得明文记录或返回。
- 任何契约变更必须同步更新设计文档、追踪矩阵、OpenAPI、迁移、测试和决策记录。
- 正式硬件支持声明必须通过 `hardware-targets.json` 定义的逐能力真机矩阵；模拟器、按钮存在或同系列其他型号资料不能代替真机证据。
- 发现设计矛盾时先停止扩展实现，在 `docs/DECISION_LOG.md` 增加待决策项，不得自行绕开约束。

## 完成定义

一个功能只有同时满足以下条件才算完成：

- 对应需求编号明确；
- 真实适配路径和失败语义明确；
- API 与界面行为一致；
- 权限、审计、超时和重试策略已实现；
- 单元、契约、集成和必要的真机验收通过；
- 文档和追踪矩阵已更新。
